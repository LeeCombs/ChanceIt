#include <stdio.h>
#include "probabilities.h"

/**
 * Sign in a player
 *
 * name The name to sign in with
 */
void signIn_UI(char* name) {
    // Sign in a player
    // If it's the first time it's called, set player 1
    // otherwise set player 2
}

/**
 * Tell the game to roll
 */
void roll_UI(){
    // Roll for a game
}

/**
 * Tell the game to end the current turn
 */
void endTurn_UI(){
    // End the current game's turn
}

/**
 *
 */
void getGameScores(){
    // Return both player's scores
}
